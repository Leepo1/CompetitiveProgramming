#include <iostream>
#include <fstream>
#include <algorithm>
#include <cmath>
#include <math.h>
#include <vector>
#include <map>
#include <string>
#include <cstdio>
#include <stdlib.h>
#include <utility>
using namespace std;

int main(){
// binary_search(v, v+n, value)
// nextpermutation(v,v+n);
// str.replace(interval of first string, interval of second string)
//String Processing


//string_name.rfind("......",0) ==0 (to check if a string starts with a string "......" to stop reading input)
// find(str) (to find occurance of substring inside of a string)
// find(str, pos) (to to find next occurance)
// tolower() toupper() isalpha() isdigit()
// to check if vowel create string vowel ="aeiou"
// to tokenize string (substrings) stringstream stored in vector<string> tokens




  return 0;
}
